var figure = figure || (function(win, doc){
    var init, resizer, scroller;

    resizer = function(){

    };
    init = function(){
        //console.log(win);
    };

    return {
        init:init
    }
})(window, document);